
Permissions

Use Credentials.cs for inserting credentials to StartProcess()
When debugging and credentials are set to the logged on admin, then can start process on same session
But when as WinService and service is set on same account, then processes are run on a different session


Set-ExecutionPolicy for running Powershell scripts
